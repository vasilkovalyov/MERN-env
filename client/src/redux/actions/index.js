import {
    login_user,
    logout_user,
    register_user,
    set_avatar_user
} from './auth'

const actions = {
    login_user,
    logout_user,
    register_user,
    set_avatar_user
}

export default actions;